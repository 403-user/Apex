[package]
name = "apex-protocol"
version = "0.1.0"
edition = "2021"
description = "Wire protocol definitions for client-server IPC"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
anyhow = "1"
